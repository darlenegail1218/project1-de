pipeline {
    agent any

    stages {
        stage('Version Tracker') {
            steps {
                sh """
                    git checkout -b v1.1
                    git push origin v1.1
                """
            }
        }
    }
    post {
        // Clean after build
        always {
            cleanWs()
        }
    }
}
