pipeline {
    agent any

    stages {
        stage('Version Tracker') {
            steps {
                sh """
                    git checkout -b v1.1
                    git remote set-url origin https://darlenegail1218:toby0113@github.com/darlenegail1218/project1-de.git
                    git push origin v1.1
                """
            }
        }
    }
    post {
        // Clean after build
        always {
            cleanWs()
        }
    }
}
