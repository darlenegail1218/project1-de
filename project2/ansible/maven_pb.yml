---
- name: Maven Build
  hosts: localhost
  #become: yes

  tasks:
      - name: maven build command
        ansible.builtin.command: | 
                cd ../webapp 
                mvn -f pom.xml clean package
